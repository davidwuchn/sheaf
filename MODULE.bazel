module(
    name = "sheaf",
    version = "2.2.0",
)

bazel_dep(name = "platforms", version = "1.1.0")
bazel_dep(name = "rules_cc", version = "0.2.17")
bazel_dep(name = "rules_rust", version = "0.73.0")

rust = use_extension("@rules_rust//rust:extensions.bzl", "rust")
rust.toolchain(
    edition = "2024",
    versions = ["1.95.0"],
)
use_repo(rust, "rust_toolchains")
register_toolchains("@rust_toolchains//:all")

crate = use_extension("@rules_rust//crate_universe:extensions.bzl", "crate")
crate.from_cargo(
    name = "crates",
    cargo_lockfile = "//sheaf:Cargo.lock",
    lockfile = "//:cargo-bazel-lock.json",
    manifests = ["//sheaf:Cargo.toml"],
    supported_platform_triples = [
        "aarch64-apple-darwin",
        "aarch64-unknown-linux-gnu",
        "x86_64-unknown-linux-gnu",
    ],
)
use_repo(crate, "crates")

iree_runtime_repository = use_repo_rule(
    "//bazel:iree_runtime_repository.bzl",
    "iree_runtime_repository",
)
iree_runtime_repository(name = "iree_runtime")

iree_compiler_repository = use_repo_rule(
    "//bazel:iree_compiler_repository.bzl",
    "iree_compiler_repository",
)
iree_compiler_repository(name = "iree_compiler")
